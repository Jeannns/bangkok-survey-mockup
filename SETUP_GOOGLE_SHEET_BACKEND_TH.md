# การเชื่อมต่อ RP mockup กับ Google Sheet

ตอนนี้ `survey_mockup.html` สามารถบันทึกคำตอบจริงลง Google Sheet ได้แล้ว แทนที่จะรันอยู่ใน memory เฉยๆ ค่าเริ่มต้นยังปิดอยู่ (`API_URL` เป็นค่าว่าง ปุ่ม Save/Submit จะขึ้นข้อความ "โหมดทดลอง - ยังไม่ได้บันทึก") ทำตามขั้นตอนด้านล่างเพื่อเปิดใช้งานจริง

## หลักการทำงาน

- แบบสอบถามใช้ **ลิงก์เดียวสำหรับทุกคน** (ไม่ได้สร้างลิงก์แยกต่อครัวเรือนล่วงหน้า เพราะไม่มี list รายชื่อครัวเรือนที่ฟิกไว้ให้แจก)
- ครั้งแรกที่มีคนเปิดหน้านี้ ระบบจะสุ่มสร้าง **รหัสความคืบหน้า** (เช่น `XK7T-2931`) แล้วเก็บไว้ใน `localStorage` ของเบราว์เซอร์นั้น รหัสนี้จะโชว์อยู่ที่หน้า Welcome
- กด **Save draft** จะส่งคำตอบปัจจุบันไปที่ Sheet พร้อมแนบรหัสนี้ เปิดแบบสอบถามซ้ำบนอุปกรณ์/เบราว์เซอร์เดิมจะทำต่อได้อัตโนมัติ ถ้าเปลี่ยนอุปกรณ์ ก็แค่พิมพ์รหัสเดิมลงในช่อง "ทำต่อ" ที่หน้า Welcome เพื่อดึงคำตอบกลับมา
- กด **Submit Survey** จะส่งข้อมูลฉบับสมบูรณ์ครั้งสุดท้าย แล้วเปลี่ยนสถานะแถวนั้นเป็น `submitted`
- เนื่องจากไม่มี list ครัวเรือนที่ลงทะเบียนไว้ล่วงหน้า การกันตอบซ้ำจึงเป็นแบบ "หลวมๆ" เท่านั้น: มีการเก็บ flag ไว้ใน `localStorage` ของเบราว์เซอร์นั้นว่าเคย submit แล้ว (กันคนเดิมกดส่งซ้ำโดยไม่ตั้งใจ) แต่ไม่มีอะไรกันคนที่ใช้อุปกรณ์อื่นมาตอบซ้ำ วิธีจัดการที่แนะนำคือไปเช็คตอนทำความสะอาดข้อมูล (เช่น ดูว่ามีแถวที่องค์ประกอบครัวเรือน + ตำแหน่งบ้านตรงกันผิดปกติหรือไม่)

## ขั้นตอนการติดตั้ง

1. **สร้าง Google Sheet ใหม่** ตั้งชื่ออะไรก็ได้ (เช่น "Bangkok School Escorting Survey - Responses") ปล่อยว่างไว้ได้เลย - สคริปต์จะสร้างแท็บ "Responses" ให้เองอัตโนมัติตอนรันครั้งแรก
2. ในชีตนั้น ไปที่เมนู **Extensions > Apps Script**
3. ลบเนื้อหา `Code.gs` เดิมทิ้ง แล้ว paste เนื้อหาจากไฟล์ `google_apps_script/Code.gs` ใน repo นี้ลงไปแทน
4. กด **Deploy > New deployment**
   - เลือกประเภท: **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**
5. กด **Deploy** แล้วกด authorize สคริปต์เมื่อระบบถาม (สคริปต์นี้ขอสิทธิ์แค่เข้าถึงชีตนี้ชีตเดียว) จากนั้น copy **Web app URL** ที่ได้มา (ลงท้ายด้วย `/exec`)
6. เปิด `survey_mockup.html` หาบรรทัดนี้ใกล้ๆ ต้นของ `<script>`:
   ```js
   const API_URL = ''; // <- paste your Apps Script Web App URL here after deploying
   ```
   แล้ว paste URL ที่ได้ลงไปในเครื่องหมายคำพูด
7. Reload หน้าเว็บ ลองกรอกคำตอบ 2-3 ข้อ กด **Save draft** แล้วไปเช็คใน Sheet - ควรมีแถวใหม่ขึ้นในแท็บ "Responses" พร้อมรหัสความคืบหน้าของคุณ และคอลัมน์ `data_json` ที่เก็บคำตอบไว้

## ถ้าแก้แบบสอบถามภายหลัง

คำตอบทุกอย่างถูกเก็บเป็น JSON blob ก้อนเดียวต่อ 1 แถว ไม่ได้แยกเป็นคอลัมน์ต่อคำถามในสเปรดชีต ดังนั้นการเพิ่ม/แก้ชื่อ/ลบคำถามใน mockup จะไม่ต้องแตะ `Code.gs` หรือคอลัมน์ในชีตเลย พอถึงเวลาจะวิเคราะห์ข้อมูลจริง ให้ใช้ `google_apps_script/flatten_responses.py` เพื่อแตก JSON นั้นออกเป็นคอลัมน์แยกต่อคำถาม:

```
# ในชีต: File > Download > Comma Separated Values (.csv)
python3 flatten_responses.py responses_raw.csv responses_flat.xlsx
```

## การ deploy ใหม่หลังแก้ Code.gs

ถ้าแก้ `Code.gs` ภายหลัง ต้องเข้า **Deploy > Manage deployments > กดไอคอนดินสอ (Edit) > New version** การเปลี่ยนแปลงถึงจะมีผลจริง - แค่กด save ในตัว editor เฉยๆ ไม่พอ URL ที่ใช้งานอยู่จะไม่อัปเดตให้เอง

## ข้อจำกัดที่ควรทบทวนก่อนลงสนามจริง

- **เรื่อง deploy ใหม่ด้านบน** - ลืมง่าย แล้วอาจ debug ผิดเวอร์ชันได้
- **ความเป็นส่วนตัวของพิกัดบ้าน** - `data_json` จะเก็บพิกัดตามความละเอียดที่ตัวเลือกที่อยู่บ้านจับได้ ต้องตัดสินใจก่อนลงสนามว่าจะเก็บตามนั้นเลย หรือลดความละเอียดลง (ในแบบสอบถามเองก็มีโน้ตแนะนำให้ anonymize จนถึงระดับต่ำกว่าชื่อถนน หรือใช้ระดับเขต/รหัสไปรษณีย์แทน)
- **โควต้าของ Apps Script** - เพียงพอสำหรับขนาดงานนี้ (500-1,000 คน) แต่แต่ละบัญชี Google มีโควต้าการรันสคริปต์ต่อวัน ถ้าบัญชีนั้นมีสคริปต์อื่นที่ใช้งานหนักอยู่ด้วย ควรเช็คโควต้าระหว่างเก็บข้อมูลภาคสนามด้วย
- **ไม่มีการบังคับ "หนึ่งลิงก์ต่อหนึ่งคน"** - เพราะไม่มี list ครัวเรือน ใครก็ตามที่มีลิงก์สามารถเริ่มตอบได้ นี่เป็นข้อแลกเปลี่ยนที่ตั้งใจไว้ (ดูรายละเอียดการตัดสินใจนี้ในบทสนทนาที่นำมาสู่ไฟล์นี้)
