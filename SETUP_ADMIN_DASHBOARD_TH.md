# การตั้งค่า admin dashboard แบบจำกัดสิทธิ์

นี่คือเครื่องมือ**แยกต่างหาก**จากตัวแบบสอบถาม สำหรับให้คุณเช็คข้อมูลที่มีคนตอบเข้ามาแบบส่วนตัว (จำนวนรวม, กราฟที่ filter/group ได้, ตารางคำตอบล่าสุด) ระหว่างเก็บข้อมูลภาคสนาม มันอ่านข้อมูลจาก Google Sheet ชีตเดียวกับที่แบบสอบถามเขียนลงไป แต่อยู่คนละ URL และ deploy โดยจำกัดสิทธิ์ให้เข้าได้แค่บัญชี Google ของคุณเท่านั้น ใครก็ตามที่มีลิงก์แบบสอบถามจะหา/เข้าหน้านี้ไม่ได้เลย เพราะไม่มีการเชื่อมโยงกันเลย

## ทำไมต้องแยกเป็น Apps Script คนละโปรเจกต์

backend ของแบบสอบถามเอง (`google_apps_script/Code.gs`) ต้อง deploy แบบ **Who has access: Anyone** เพราะผู้ตอบจริงๆ (ที่ไม่ได้ login อะไรเลย) ต้องเข้าถึงได้ ส่วน dashboard นี้ต้องการตรงกันข้ามคือ **Who has access: Only myself** การตั้งค่าสิทธิ์ของ Apps Script มีผลกับทั้ง deployment ไม่ได้แยกเป็นรายฟังก์ชันภายในสคริปต์เดียวกัน ดังนั้นวิธีเดียวที่ปลอดภัยจริงๆ ในการมี endpoint หนึ่งที่เป็น "Anyone" กับอีกอันที่เป็น "Only myself" คือแยกเป็นคนละโปรเจกต์ Apps Script ไปเลย นี่คือเหตุผลที่ `Dashboard.gs` เป็นไฟล์/โปรเจกต์ของตัวเอง ไม่ได้เพิ่มเข้าไปใน `Code.gs`

## ขั้นตอนการติดตั้ง

1. เปิด Google Sheet ชีตเดียวกับที่ backend ของแบบสอบถามเขียนข้อมูลลงไป (ชีตที่คุณ deploy `Code.gs` ไว้) copy **Sheet ID** จาก address bar ของเบราว์เซอร์:
   ```
   https://docs.google.com/spreadsheets/d/  รหัสยาวๆ ตรงนี้  /edit
   ```
2. ไปที่ **script.google.com** (รอบนี้ไม่ต้องเข้าผ่านเมนู Extensions ในชีต ให้เปิดโปรเจกต์ใหม่แบบ standalone) แล้วกด **New project**
3. ลบโค้ดตัวอย่างเดิมทิ้ง แล้ว paste เนื้อหาจากไฟล์ `google_apps_script_dashboard/Dashboard.gs` ใน repo นี้ลงไปแทน
4. หาบรรทัดนี้ใกล้ๆ ต้นไฟล์ แล้ว paste Sheet ID จากข้อ 1 ลงไป:
   ```js
   const SHEET_ID = ''; // <- paste the Google Sheet ID here (the long id in its URL)
   ```
5. กด **Deploy > New deployment**
   - เลือกประเภท: **Web app**
   - Execute as: **Me**
   - Who has access: **Only myself** (จุดนี้สำคัญมาก ต่างจากตอน deploy ของแบบสอบถามเอง ห้ามตั้งเป็น "Anyone" เด็ดขาด)
6. กด **Deploy** แล้ว authorize เมื่อระบบถาม จากนั้น copy **Web app URL**
7. เปิด URL นั้นในเบราว์เซอร์ เพราะจำกัดสิทธิ์ไว้แค่บัญชีคุณ Google อาจถามให้ยืนยัน/login ก่อน ซึ่งเป็นเรื่องปกติ จากนั้นจะเห็นหน้า dashboard: จำนวนรวม/ที่ส่งแล้ว/ที่ยังทำค้างไว้, กราฟที่ group/filter ได้ตามคำถามที่มีคนตอบ, และตารางคำตอบล่าสุด
8. Bookmark URL นี้ไว้ เป็นของคุณคนเดียว ไม่ปรากฏอยู่ที่ไหนในหน้าแบบสอบถามสาธารณะเลย

## วิธีใช้งาน dashboard

- **Group by**: เลือกคำถามข้อไหนก็ได้ เพื่อดูกราฟแท่งว่าแต่ละคำตอบมีคนตอบกี่คน
- **Filter by**: จะเลือก filter ตามคำตอบของอีกคำถามหนึ่งก่อนก็ได้ (เช่น filter `income = 15,000-30,000` แล้วค่อย group by `escort_mode`)
- **Include**: เลือกดูเฉพาะที่ submit แล้ว (ค่าเริ่มต้น) หรือรวม draft ที่ยังทำค้างไว้ด้วย
- **Auto-refresh**: เปิดอยู่โดยค่าเริ่มต้น ดึงข้อมูลใหม่ทุก 30 วินาที ปิดได้ที่ checkbox หรือกด "Refresh now" เมื่อไหร่ก็ได้
- รายการคำถามใน dropdown จะถูกค้นหาแบบไดนามิกจากข้อมูลจริงที่มีอยู่ ดังนั้นถ้าเพิ่ม/ลดคำถามในแบบสอบถามทีหลัง dropdown จะอัปเดตให้เองโดยไม่ต้องแก้อะไรตรงนี้

## เพิ่มสิทธิ์ให้ทีมทีหลัง

พอพร้อมจะเพิ่มคนอื่นแล้ว: **Deploy > Manage deployments > กดไอคอนดินสอ (Edit) > Who has access** เปลี่ยนเป็นระบุอีเมลเฉพาะคน (ต้องมีบัญชี Google) หรือถ้าทุกคนอยู่ใน Google Workspace องค์กรเดียวกัน จะเลือก "Anyone within [ชื่อองค์กร]" ก็ได้ แล้วกด deploy เวอร์ชันใหม่เพื่อให้มีผล

## หมายเหตุ

- dashboard นี้แค่อ่านข้อมูลเท่านั้น ไม่เคยเขียนอะไรกลับลงชีต จึงไม่มีทางไปทำข้อมูลคำตอบเสียหายหรือถูกเขียนทับ
- ถ้าแก้ `Dashboard.gs` ภายหลัง กฎเดียวกับฝั่ง backend ของแบบสอบถาม: แค่กด save ในตัว editor ไม่พอ URL ที่ใช้งานอยู่จะไม่อัปเดตให้เอง ต้องเข้า **Manage deployments > Edit > New version** ด้วย
